package Assets;

import java.awt.*;

public class Block {

    private int x1, y1;
    private int x2, y2;
    private int length;

    /*
     * x1, y1 are the x and y locations of the "top left" of a LINE
     * x2, y2 are the x and y locations of the "bottom right" of a LINE
     * length is how many "layers" the resulting line has– forming a 2d block instead of a 1d line
     */
    public Block(int x1, int y1, int x2, int y2, int length) {

    }
}
